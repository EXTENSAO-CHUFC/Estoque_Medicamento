-- O schema principal é criado pelo SQLAlchemy em app.models.init_db.
-- Este arquivo fica reservado para extensões/configurações específicas do PostgreSQL analítico.
CREATE EXTENSION IF NOT EXISTS pg_stat_statements;
